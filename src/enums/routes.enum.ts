export enum Routes {
  HOME = "/",
  HISTORY = "/history",
  CHARACTERS = "/characters",
  LOCATIONS = "/locations",
  ARTIFACTS_AND_ARMORY = "/artifacts_and_armory",
}

export enum SectionsRoutes {
  HISTORY = Routes.HISTORY,
  CHARACTERS = Routes.CHARACTERS,
  LOCATIONS = Routes.LOCATIONS,
  ARTIFACTS_AND_ARMORY = Routes.ARTIFACTS_AND_ARMORY,
}
